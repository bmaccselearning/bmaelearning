<?php


// กำหนดเวอร์ชันปัจจุบันของ Plugin
$plugin->version = 2024021300; // วันที่เปลี่ยนแปลง: YYYYMMDDHHMM (Year, Month, Day, Hour, Minute)
$plugin->requires  = 2023100400; // Requires this Moodle version.

// กำหนดเวอร์ชันของ Moodle ที่ Plugin รองรับ (เวอร์ชันต่ำสุดและสูงสุด)
$plugin->requires  = 2023100400; // Requires this Moodle version.
$plugin->component = 'tool_bangkok'; // ชื่อของโมดูลหรือเครื่องมือ

// กำหนดเวอร์ชัน Moodle ที่ Plugin ถูกทดสอบแล้ว
$plugin->maturity = MATURITY_STABLE; // ระดับความสมบูรณ์: MATURITY_ALPHA, MATURITY_BETA, MATURITY_RC, MATURITY_STABLE

// กำหนดเวอร์ชันและเวลาที่เพิ่มขึ้นทุกครั้งเมื่อมีการปรับปรุง Plugin
$plugin->release = '1.0.0'; // เวอร์ชันการเผยแพร่ (Release Version)
$plugin->versiondate = '2024-02-13'; // วันที่เผยแพร่เวอร์ชัน (Release Date)


